# ---------------------------------------------------------
# This file is managed by Ansible, manual changes will be
# overwritten on the next run.
# ---------------------------------------------------------

# Bloquer tout répertoire .git, quel que soit le chemin

location ~* /\.git(/|$) {
    deny all;
}

# Bloquer tous les fichiers de config Git (gitignore, gitattributes, etc.)
# en prenant en compte les préfixes/suffixes avec -, _, ou .

location ~* /[._-]*git(ignores?|attributes|modules|config|credentials|keep)[._-]*$ {
    deny all;
}
