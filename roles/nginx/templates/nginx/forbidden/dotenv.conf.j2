# ---------------------------------------------------------
# This file is managed by Ansible, manual changes will be
# overwritten on the next run.
# ---------------------------------------------------------

# Bloque tous les fichiers .env, avec éventuellement
# des préfixes/suffixes contenant -, _, ou .

location ~* /\.env {
    deny all;
}

location ~* /(^|\.)env($|\.) {
    deny all;
}
